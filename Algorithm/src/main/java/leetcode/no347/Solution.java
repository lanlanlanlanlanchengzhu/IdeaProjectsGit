package leetcode.no347;

import java.util.List;

/**
 * @see <href="https://leetcode-cn.com/problems/top-k-frequent-elements/"></href="">
 */
public class Solution {

    /**
     * Given a non-empty array of integers, return the k most frequent elements.
     * // 1 统计每个元素出现次数
     * // 2 构造k个元素的优先队列
     * // 3 遍历所有元素对应的出现次数
     * // 4 出队，获取出现次数top k元素
     *
     * 时间复杂度 O(nlgk) 每个元素入优先队列开销
     * 空间复杂度 O(n) 哈希表开销
     *
     * @param nums
     * @param k
     * @return
     */
    public List<Integer> topKFrequent(int[] nums, int k) {

        return null;
    }

}
